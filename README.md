# LATTICE-Swarm

User-facing LATTICE entrypoint.

This repository owns the CLI and TUI surfaces. Runtime execution, bus, model transport, Python bindings and plugin contracts live in `LATTICE-Runtime`. Official plugin implementations live in `LATTICE-Plugins`.

License: Apache-2.0.
